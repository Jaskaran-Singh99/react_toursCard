import React from "react";

export default function Loading(){
    const loading= {
        textAlign:'center',
        marginTop:'50px'
    }
    return(
        <h1 style={loading}>Loading...</h1>
    )
}